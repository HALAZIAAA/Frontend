function AuthDivider() {
  return (
    <div className="auth-divider" role="separator" aria-label="소셜 로그인과 이메일 로그인 구분선">
      <span className="auth-divider-line" aria-hidden="true" />
      <span className="auth-divider-text">또는</span>
      <span className="auth-divider-line" aria-hidden="true" />
    </div>
  )
}

export default AuthDivider
